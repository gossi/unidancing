{{page-title 'Fertigkeiten'}}

<h1>Fertigkeiten</h1>

<ul>
  {{#each @model as |entry|}}
    <li><LinkTo
        @route='skills.details'
        @model={{entry.id}}
      >{{entry.title}}</LinkTo></li>
  {{/each}}
</ul>